{
  "name": "elephantdiesel",
  "version": "1.0.0",
  "description": "gulp-liverload 和 liverload chrome实现热更新 1. `npm install` 安装依赖包 2. `node app.js` 或 `npm start` 运行项目于localhost:4000 3. `gulp watch` 开启热更新,开启liverload chrome 插件 4. `gulp dist` 打包代码",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "alloytouch": "^0.2.5",
    "body-parser": "^1.18.3",
    "express": "^4.17.3",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-livereload": "^4.0.1",
    "gulp-replace": "^1.0.0"
  }
}
